{
  "name": "agentgym",
  "version": "0.4.0",
  "description": "Train coding agents on your own codebase: benchmark, evolve instructions, validate, retest.",
  "type": "module",
  "bin": { "agentgym": "./src/cli.js" },
  "scripts": { "test": "node --test", "start": "node src/cli.js", "check": "node --check src/cli.js && node --check src/core.js && node --check src/mutations.js" },
  "keywords": ["ai", "coding-agent", "agents", "benchmark", "agents-md", "agent-skills", "codex", "evaluation"],
  "license": "MIT",
  "engines": { "node": ">=20" }
}
