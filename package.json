{
  "name": "kodematik",
  "version": "0.6.12",
  "description": "Train coding agents with cost-aware historical test oracles, bounded qualification, job-scoped CI reconstruction, exact task-set integrity, and reproducibility guards.",
  "type": "module",
  "bin": { "kodematik": "./src/cli.js" },
  "scripts": {
    "test": "node --test",
    "start": "node src/cli.js",
    "check": "node --check src/cli.js && node --check src/core.js && node --check src/qualification.js && node --check src/task-quality.js && node --check src/integrity.js && node --check src/runtime.js && node --check src/dependencies.js && node --check src/verification.js && node --check src/evaluation.js && node --check src/mutations.js && node --check src/agents/openrouter.js"
  },
  "keywords": ["ai", "coding-agent", "agents", "benchmark", "regression", "agents-md", "agent-skills", "codex", "openrouter", "deepseek", "evaluation"],
  "license": "MIT",
  "engines": { "node": ">=20" }
}
